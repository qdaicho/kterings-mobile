// config.js
const config = {
  // Add your configuration values here
  // For example:
  apiUrl: 'your-api-url',
  environment: process.env.NODE_ENV || 'development',
  // ... other config values
};

// Both export methods for maximum compatibility
module.exports = config;
export default config;
